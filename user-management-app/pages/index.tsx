// pages/index.tsx
import React, { useEffect, useState } from 'react';
import UserList from '../components/UserList'; // Make sure this import is correct
import UserDetail from '../components/UserDetail';
import { User } from '../components/types';

// ... (rest of the code)

const Home: React.FC = () => {
  // ... (rest of the code)
};

export default Home;
